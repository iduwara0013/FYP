# Backend Workspace

This folder groups backend services in a monorepo-style structure.

## Planned layout

- `Spring_Backend` for Firebase connection, authentication, and Firestore APIs
- `python_backend` for ML models and agentic AI logic

## Current backend split

- `Spring_Backend` handles application data, Firebase integration, and business APIs
- `python_backend` handles predictions, recommendations, and AI-driven logic
