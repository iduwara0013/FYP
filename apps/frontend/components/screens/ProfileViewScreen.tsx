import { MaterialCommunityIcons } from "@expo/vector-icons";
import React, { useState } from "react";
import { ActivityIndicator, Alert, ScrollView, StyleSheet, Switch, Text, TextInput, TouchableOpacity, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { ScreenHeader } from "@/components/ui/ScreenHeader";
import { useTheme } from "@/context/ThemeContext";
import { updateProfile } from "@/lib/spring-api";
import type { ProfileData } from "./profile-types";

type Props = { profile: ProfileData; onBackToHome: () => void; onProfileUpdated?: (profile: ProfileData) => void };
export function ProfileViewScreen({ profile, onBackToHome, onProfileUpdated }: Props) {
  const { theme }=useTheme(); const { colors }=theme; const [draft,setDraft]=useState(profile); const [editing,setEditing]=useState(false); const [saving,setSaving]=useState(false);
  const set=(key:string,value:string|number|boolean|undefined)=>setDraft(current=>({...current,[key]:value}) as ProfileData);
  const save=async()=>{if(!draft.fullName.trim()||!draft.phoneNumber.trim()||!draft.address.trim()||!draft.region.trim()){Alert.alert("Missing details","Name, phone, address and region are required.");return;}setSaving(true);try{const updated=await updateProfile(draft);setDraft(updated);onProfileUpdated?.(updated);setEditing(false);Alert.alert("Profile updated","Your changes were saved successfully.");}catch(reason){Alert.alert("Update failed",reason instanceof Error?reason.message:"Could not update profile.");}finally{setSaving(false);}};
  const action=editing?<TouchableOpacity disabled={saving} onPress={save} style={[styles.action,{backgroundColor:colors.primary}]}>{saving?<ActivityIndicator size="small" color={colors.primaryContrast}/>:<><MaterialCommunityIcons name="check" size={18} color={colors.primaryContrast}/><Text style={{color:colors.primaryContrast,fontWeight:"800"}}>Save</Text></>}</TouchableOpacity>:<TouchableOpacity onPress={()=>setEditing(true)} style={[styles.action,{backgroundColor:colors.primarySoft}]}><MaterialCommunityIcons name="pencil-outline" size={18} color={colors.primary}/><Text style={{color:colors.primary,fontWeight:"800"}}>Edit</Text></TouchableOpacity>;
  return <SafeAreaView edges={["top"]} style={[styles.safe,{backgroundColor:colors.background}]}><ScreenHeader title="My profile" subtitle={draft.role==="farmer"?"Farmer account":"Buyer account"} icon={draft.role==="farmer"?"sprout":"storefront-outline"} onBack={editing?()=>{setDraft(profile);setEditing(false);}:onBackToHome} action={action}/><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={[styles.identity,{backgroundColor:colors.surface,borderColor:colors.border},theme.shadows.soft]}><View style={[styles.avatar,{backgroundColor:colors.primarySoft}]}><MaterialCommunityIcons name={draft.role==="farmer"?"sprout":"storefront-outline"} size={31} color={colors.primary}/></View><View style={{flex:1}}><Text style={[styles.name,{color:colors.text}]}>{draft.fullName}</Text><Text style={[styles.email,{color:colors.textMuted}]}>{draft.email}</Text><Text style={[styles.code,{color:colors.primary}]}>{draft.role==="farmer"?draft.farmerCode:draft.buyerCode}</Text></View></View><Section title="Contact details" colors={colors}><Field label="Full name" value={draft.fullName} editing={editing} onChange={v=>set("fullName",v)} colors={colors}/><Field label="Email (login account)" value={draft.email} editing={false} onChange={()=>{}} colors={colors}/><Field label="Phone number" value={draft.phoneNumber} editing={editing} keyboard="phone-pad" onChange={v=>set("phoneNumber",v)} colors={colors}/><Field label="Address" value={draft.address} editing={editing} multiline onChange={v=>set("address",v)} colors={colors}/><Field label="Region / district" value={draft.region} editing={editing} onChange={v=>set("region",v)} colors={colors}/></Section>{draft.role==="farmer"?<Section title="Farm details" colors={colors}><Field label="National ID" value={draft.nationalId??""} editing={editing} onChange={v=>set("nationalId",v)} colors={colors}/><Field label="Farmer type" value={draft.farmerType} editing={editing} onChange={v=>set("farmerType",v)} colors={colors}/><Field label="Total land area (ha)" value={draft.totalLandArea?.toString()??""} editing={editing} keyboard="decimal-pad" onChange={v=>set("totalLandArea",v?Number(v):undefined)} colors={colors}/><Field label="Experience (years)" value={draft.experienceYears?.toString()??""} editing={editing} keyboard="number-pad" onChange={v=>set("experienceYears",v?Number(v):undefined)} colors={colors}/><Toggle label="Irrigation available" value={draft.hasIrrigation} editing={editing} onChange={v=>set("hasIrrigation",v)} colors={colors}/></Section>:<Section title="Buyer details" colors={colors}><Field label="Buyer type" value={draft.buyerType} editing={editing} onChange={v=>set("buyerType",v)} colors={colors}/><Field label="Organization" value={draft.organizationName??""} editing={editing} onChange={v=>set("organizationName",v)} colors={colors}/><Field label="Preferred crop" value={draft.preferredCrop??""} editing={editing} onChange={v=>set("preferredCrop",v)} colors={colors}/><Field label="Required quantity (kg)" value={draft.requiredQuantity?.toString()??""} editing={editing} keyboard="decimal-pad" onChange={v=>set("requiredQuantity",v?Number(v):undefined)} colors={colors}/><Field label="Notes" value={draft.notes??""} editing={editing} multiline onChange={v=>set("notes",v)} colors={colors}/><Toggle label="Storage available" value={draft.hasStorage} editing={editing} onChange={v=>set("hasStorage",v)} colors={colors}/><Toggle label="Transport available" value={draft.hasTransport} editing={editing} onChange={v=>set("hasTransport",v)} colors={colors}/></Section>}</ScrollView></SafeAreaView>;
}
function Section({title,children,colors}:{title:string;children:React.ReactNode;colors:any}){return <View style={[styles.section,{backgroundColor:colors.surface,borderColor:colors.border}]}><Text style={[styles.sectionTitle,{color:colors.text}]}>{title}</Text>{children}</View>}
function Field({label,value,editing,onChange,colors,multiline,keyboard}:{label:string;value:string;editing:boolean;onChange:(v:string)=>void;colors:any;multiline?:boolean;keyboard?:"phone-pad"|"decimal-pad"|"number-pad"}){return <View style={styles.field}><Text style={[styles.label,{color:colors.textMuted}]}>{label}</Text>{editing?<TextInput value={value} onChangeText={onChange} multiline={multiline} keyboardType={keyboard} style={[styles.input,multiline&&styles.multiline,{color:colors.text,backgroundColor:colors.background,borderColor:colors.border}]}/>:<Text style={[styles.value,{color:colors.text}]}>{value||"Not provided"}</Text>}</View>}
function Toggle({label,value,editing,onChange,colors}:{label:string;value:boolean;editing:boolean;onChange:(v:boolean)=>void;colors:any}){return <View style={styles.toggle}><View><Text style={[styles.label,{color:colors.textMuted}]}>{label}</Text><Text style={[styles.value,{color:colors.text}]}>{value?"Available":"Not available"}</Text></View>{editing?<Switch value={value} onValueChange={onChange} trackColor={{false:colors.border,true:colors.primarySoft}} thumbColor={value?colors.primary:colors.textMuted}/>:null}</View>}
const styles=StyleSheet.create({safe:{flex:1},content:{padding:16,paddingBottom:48,gap:14},action:{height:42,paddingHorizontal:13,borderRadius:14,flexDirection:"row",alignItems:"center",justifyContent:"center",gap:5},identity:{borderWidth:1,borderRadius:22,padding:18,flexDirection:"row",alignItems:"center",gap:14},avatar:{width:64,height:64,borderRadius:20,alignItems:"center",justifyContent:"center"},name:{fontSize:21,fontWeight:"900"},email:{fontSize:12,marginTop:3},code:{fontSize:11,fontWeight:"800",marginTop:5},section:{borderWidth:1,borderRadius:20,padding:17},sectionTitle:{fontSize:16,fontWeight:"900",marginBottom:8},field:{paddingVertical:10,borderTopWidth:StyleSheet.hairlineWidth,borderTopColor:"rgba(128,128,128,.22)"},label:{fontSize:11,textTransform:"uppercase",letterSpacing:.4,fontWeight:"700"},value:{fontSize:15,fontWeight:"600",marginTop:4},input:{minHeight:46,borderWidth:1,borderRadius:12,paddingHorizontal:12,marginTop:6,fontSize:15},multiline:{minHeight:82,paddingTop:12,textAlignVertical:"top"},toggle:{paddingVertical:11,borderTopWidth:StyleSheet.hairlineWidth,borderTopColor:"rgba(128,128,128,.22)",flexDirection:"row",alignItems:"center",justifyContent:"space-between"}});
